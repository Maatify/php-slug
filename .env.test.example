# Local-only MySQL-compatible integration database. The database name MUST end in _test.
SLUG_TEST_DB_HOST=127.0.0.1
SLUG_TEST_DB_PORT=3307
SLUG_TEST_DB_NAME=maatify_slug_test
SLUG_TEST_DB_USER=slug_test
SLUG_TEST_DB_PASSWORD=slug_test_password
