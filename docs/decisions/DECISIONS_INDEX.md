# Decision Index

This file is the canonical current-state discovery index for durable engineering decisions owned by `Maatify/php-slug`.

It is a discovery registry only. It is not a Decision Record, does not duplicate decision rationale, does not replace `STANDARDS_MANIFEST.md`, and does not create a new engineering policy.

## Current State

The active built-in profile runtime compatibility decision is registered below.

## Registered Decisions

- [DEC-001 — Built-in Profile Runtime Compatibility](DEC-001_BUILTIN_PROFILE_RUNTIME_COMPATIBILITY.md) — `ACTIVE`; capability and observable-behavior admission for `ascii-v1` and `unicode-v1`.
